# dependencies / build output
node_modules/
dist/
.astro/
app/src/generated/
*.log

# raw source inputs — not published (includes copyrighted course material)
_ingest/
Syllabus etc/
graph visualisation examples/
Technical glossary with 3D visualization/
*.docx
*.pdf

# os
.DS_Store
Thumbs.db

# Supabase CLI state (supabase link)
supabase/.temp/

# local agent worktrees and session state
.claude/

# Local secrets, anywhere in the tree (A89): e.g. the CLOUDFLARE_* env file for
# `supabase functions serve`. app/.gitignore covers app/; this covers the rest.
.env
.env.*
!.env.example
*.pem
*.key

# Copy to .env to build/run with accounts + synced progress (see docs/SUPABASE_SETUP.md).
# Both values are public (they ship in the browser bundle). Leave them empty for the
# local-only site: every account surface is then left out of the build.
PUBLIC_SUPABASE_URL=
PUBLIC_SUPABASE_ANON_KEY=

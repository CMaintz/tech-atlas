# Autonomous decisions & build status

Decisions taken solo during the fast-track build (per the user's "decide yourself,
log it, we review later"). Review these together after v1 is built.

## Current build status (2026-09-25)

419 terms (security 177, cs 68, platform 40, ai 134) × 2 languages, 20 articles per
language, all `draft: true`. `mise run gate` is green; the production build emits the
term, article, compare, disambiguation, A–Z, data, Explorer, Timeline, Study and review
pages plus short-name redirects, JSON/CSV/Anki exports, RSS feeds and a sitemap. Deploys
run only after the gate passes (A65). The owner's review agenda for every decision below
is `DECISIONS_REVIEW.md`.

### Status at v1.0 (2026-09-22, kept for history)

**v1 builds green.** `mise run gate` passes; the production build emits 315 pages
(124 terms × 2 languages, index, 28 compare pairs, search index) with 0 broken internal
links; `npm audit` = 0 vulnerabilities. Pipeline proven end-to-end:
bilingual YAML → Zod validation → derived `graph.json` → `/en` + `/da` static pages +
Cytoscape neighbourhood graph.

### Done

- `app/` Astro 7 scaffold: `src/schema.ts`, content layer, 3 seed security terms
  (`social-engineering`, `phishing`, `mfa`), route-based bilingual pages, faceted
  term pages, Cytoscape island, Tailwind v4, prettier.
- `app/scripts/build-graph.ts` (derives depth / inverse edges / visual weight),
  `app/scripts/lint.ts` (structural checks pass; Closed-Vocabulary E1 stubbed).
- Foundry gate: `mise.toml` (6 verbs), `.github/workflows/gate.yml` (SHA-pinned),
  `AGENTS.md` + `CLAUDE.md`.
- `design/`: full `SPEC.md`, `schema.ts`, 9 ADRs, `UNIFIED_VISION.md` decision log.

### v1 content + features — done on `feat/v1-build`

124 bilingual terms (security GRC ordliste + CS networking/OS/identity/cryptography), all
`draft: true`; search; relationship sections; compare view (28 pairs); clickable graph;
Closed Vocabulary lint (E1) + full rule set; GitHub Pages deploy.

### Next — full product (one branch + PR per feature, static-first)

1. `feat/graph-explorer` — fullscreen explorer on `graph.json` (domain/edge-type filters),
   persistent sidebar graph (SPEC §7), 3D mode with Depth as the vertical axis
   (3d-force-graph), Paths (prerequisites; route between two terms), era view.
2. `feat/articles` — optional long-form layer for ~12 terms (NIS2, GDPR, ISO 27001,
   Zero Trust, TLS, MFA, …), exempt from Closed Vocabulary; E9 already enforced.
3. `feat/study` — quizzes generated from edges (cloze over summary, contrasts, prerequisites,
   distractor filtering), per-term tracking + spaced repetition in localStorage.
4. `feat/knowledge-map` — mark Know / Familiar / Learning / Don't understand, recolour the
   graph, recommend missing prerequisites.
5. Content batch 3 — the writers' wishlist (security incident, risk assessment, RTO/RPO,
   ISMS, EDR, SOC, hardening, exploit, CVE/CVSS, data controller/processor, privileged
   account, session hijacking, …).

## Decisions log

| #   | Decision                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | Rationale                                                                                                                                                                                                                                                                                                                                                                        |
| --- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| A1  | **Working name = "Atlas"**                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | Matches the `tech-atlas` repo, the demo, and the earlier recommendation.                                                                                                                                                                                                                                                                                                         |
| A2  | **Upgraded Astro 5 → 7.3.4**                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | The 5.x line carried 1 critical + 1 high advisory (XSS/RCE); the gate's `audit` verb requires fixing, not suppressing. Scaffold re-verified green on 7.                                                                                                                                                                                                                          |
| A3  | **MIT License**                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | Sensible default matching the Foundry ecosystem; swap if you prefer another.                                                                                                                                                                                                                                                                                                     |
| A4  | **Gitignore raw source inputs** (docx / pdf / images / demo / `_ingest/` / "Syllabus etc/")                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | Keeps the repo focused on `design/` + `app/`, and avoids publishing the copyrighted course compendium.                                                                                                                                                                                                                                                                           |
| A5  | **Repo layout: app in `app/`, specs in `design/`**                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | Keeps the implementation clean and separate from the design record.                                                                                                                                                                                                                                                                                                              |
| A6  | **`test` verb = no-op, `audit` = `npm audit --audit-level=high`**                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | No tests yet (content site); `test` is a contract-required no-op until real tests exist.                                                                                                                                                                                                                                                                                         |
| A7  | **Cytoscape.js for the 2D graph; 3d-force-graph deferred for 3D**                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | Confirmed with the user; purpose-built 2D graph now, Three.js-based 3D later.                                                                                                                                                                                                                                                                                                    |
| A8  | **IDS and IPS are two terms** (the ordliste lists "IDS / IPS" on one line)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | They behave differently (watch vs block) and are a textbook "don't confuse" pair for the Compare view.                                                                                                                                                                                                                                                                           |
| A9  | **ISO 27001 and ISO 27002 are two terms** (one ordliste line)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | 27001 = management-system requirements; 27002 = control guidance. Different edges, so they pass the granularity test.                                                                                                                                                                                                                                                            |
| A10 | **`backup` and `endpoint` are single shared terms tagged `[security, cs]`**; the deliberate name collision is **`audit`** (compliance audit vs audit logging)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | Backup/endpoint mean the same thing in both fields (merge is the ADR-0003 default); `audit` genuinely differs, so it exercises namespacing.                                                                                                                                                                                                                                      |
| A11 | **`two-factor-authentication` is its own term, `kind-of mfa`**                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | The ordliste lists 2FA separately (granularity rule).                                                                                                                                                                                                                                                                                                                            |
| A12 | **Batch 2: 20 terms beyond the ordliste** (malware, asset, impact, likelihood, threat-actor, server, client, internet, HTTPS, hashing, certificates, keys, DoS, lateral movement, defence in depth, …)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | Writers kept having to write around them — the Closed Vocabulary signal that they must exist as terms.                                                                                                                                                                                                                                                                           |
| A13 | **`cryptography` added as a fourth CS cluster**                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | Keys, certificates, hashing and public-key crypto are needed to explain TLS/HTTPS/integrity.                                                                                                                                                                                                                                                                                     |
| A14 | **Closed Vocabulary base = FrequencyWords top-30k per language** (CC BY-SA 4.0), plus inflection stripping, plus Danish-only compound splitting                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | Open, per-language, frequency-ranked; English stays strict (no compound splitting).                                                                                                                                                                                                                                                                                              |
| A15 | **All content is `draft: true`**, including the three seed terms                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | LLM-drafted, not human-reviewed (pipeline rule, W5). You flip them after review.                                                                                                                                                                                                                                                                                                 |
| A16 | **Manual `[lang]` routing; dropped Astro's i18n config and config redirect**                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | Simpler with the GitHub Pages base path; root `/` meta-refreshes to `/en/`. `hreflang` alternates added for SEO.                                                                                                                                                                                                                                                                 |
| A17 | **Hosted on GitHub Pages** at `https://cmaintz.github.io/tech-atlas/`, deployed on every push to `main`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | Free for public repos; static site fits exactly.                                                                                                                                                                                                                                                                                                                                 |
| A18 | **Lint rule E5 (circular definition) deferred**                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | Needs definition-level graph analysis over the prose (which terms each definition leans on), not just edges. E1 already forces grounding in plain words; E5 is a follow-up.                                                                                                                                                                                                      |
| A19 | **Symmetric edges live on the alphabetically-first id**                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | Deterministic rule so parallel authors never write the same pair twice (lint W7 flags duplicates).                                                                                                                                                                                                                                                                               |
| A20 | **Content fixes from review:** SSL removed as a TLS alias (it is the predecessor, not a synonym); "virus" removed as a malware alias; DoS `exploits` retargeted from availability (a goal) to vulnerability; incident-reporting no longer `requires` data-breach (NIS2 covers all significant incidents).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | Found by the fresh-context content review.                                                                                                                                                                                                                                                                                                                                       |
| A21 | **Astro 7 editor JSON-schema warning left as-is**                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | Astro 7 can't generate IDE JSON schema from our zod-3 schema; validation itself works. Fix later by moving `schema.ts` to Astro's bundled zod.                                                                                                                                                                                                                                   |
| A22 | **v1.0 shipped as SPEC §11 steps 1–4, minus Articles.** Not yet shipped: long-form Articles (step 4), 3D mode on the Depth axis, Paths and era view (step 5), and the persistent/fullscreen sidebar graph (§7 — v1.0 has an inline neighbourhood graph).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | These roll into the first full-product branches; `graph.json` is already built for them. Stated plainly so the gap isn't discovered from the spec.                                                                                                                                                                                                                               |
| A23 | **`mise run test` runs the full production build** as a smoke test                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | No unit tests yet; the build catches route collisions and broken content wiring that lint/typecheck miss, so the PR gate now fails where deploy would.                                                                                                                                                                                                                           |
| A24 | **Learner progress (quizzes, knowledge map) is local-first in `localStorage`**                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | Per ADR-0008 a database arrives only with accounts, sync or semantic search.                                                                                                                                                                                                                                                                                                     |
| A25 | **Explorer: 2D (force or depth-layered) and 3D (3d-force-graph, height = Depth)**, domain + relationship-family filters, route finder (shortest path, all relationships two-way), prerequisite highlighting, `?focus=` deep links                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | SPEC §7 / §11 step 5. The canvas links every node to its page (tap = select, double-tap = open).                                                                                                                                                                                                                                                                                 |
| A26 | **Term pages: sticky sidebar graph (colour = cluster) + "What to learn first"** (every transitive prerequisite, foundations first)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | §7's persistent sidebar and the "what do I need first?" killer feature from the exploration chats.                                                                                                                                                                                                                                                                               |
| A27 | **One graph model (`src/lib/graph-model.ts`) shared by the build script and the site**; `/graph.json` is served statically                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | Removes the duplicated depth/weight logic; Explorer and CLI can't drift.                                                                                                                                                                                                                                                                                                         |
| A28 | **Era view deferred**                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | No term has an authored `era` yet; the view would be empty. Revisit when eras are authored.                                                                                                                                                                                                                                                                                      |
| A29 | **Edge families** (structure, prerequisites, contrasts, attacks & defences, regulation, lineage, used together) drive graph colour and filters                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | Seven readable groups instead of twelve raw types.                                                                                                                                                                                                                                                                                                                               |
| A30 | **Batch 3: 26 more terms** (security incident, exploit, risk assessment, CVE/CVSS, RTO/RPO, ISMS, data controller/processor, supervisory authority, certification, NIS2 entities, SoA, EDR, SOC, hardening, security monitoring, pretexting, vishing, smishing, credential stuffing, session hijacking, perimeter security, non-repudiation, privileged/service accounts, federation) + 40 enrichment edges from existing terms to them                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | The writers' wishlists — concepts they kept writing around. 150 terms total.                                                                                                                                                                                                                                                                                                     |
| A31 | **Articles for 12 terms, both languages** (NIS2, GDPR, ISO 27001, CIS Controls, Zero Trust, MFA, ransomware, phishing, risk management, incident response, TLS, encryption), rendered at `/[lang]/articles/<id>/`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | SPEC §4 depth 3; exempt from Closed Vocabulary (ADR-0004). Danish articles are written for a Danish reader, not translated.                                                                                                                                                                                                                                                      |
| A32 | **Manifest is block-style YAML, regenerated from the term files**                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | Prettier wrapped long inline entries, which broke naive appends; the term files are now the source of truth for ids.                                                                                                                                                                                                                                                             |
| A33 | **Explorer is progressive** (review fix): arriving from a term shows it plus direct edges; "Expand +1" grows one hop, "Whole map" shows everything. SPEC §7 amended to name edge families (A29) as the colour/filter unit.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | SPEC §7 said "start at the focal node; expand on demand".                                                                                                                                                                                                                                                                                                                        |
| A34 | **Study layer**: quizzes generated from edges (definition → term, "easily confused with", "what comes before", "what does X protect against / require / exploit"), distractors from the same cluster/domain, never a taxonomic relative, alternative, prerequisite or dependant                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | SPEC §9 — learning falls out of the relationships; the Atlas demo's distractor rule.                                                                                                                                                                                                                                                                                             |
| A35 | **Spaced repetition = Leitner boxes** (review after 0/1/3/7/16/35 days; a wrong answer resets to box 1), sessions put due reviews first, then unseen terms                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | Simple, explainable, good enough before any server-side scheduling.                                                                                                                                                                                                                                                                                                              |
| A36 | **Personal knowledge map**: per-term Know / Familiar / Learning / Don't understand buttons; three right answers running also counts as known; the Explorer can colour by it; "Recommended next" = unknown terms whose prerequisites are all known                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | SPEC §9 phase-after, local-first (A24).                                                                                                                                                                                                                                                                                                                                          |
| A37 | **Unit tests (Vitest) for the pure modules** (graph model, quiz generation, learner state); `mise run test` = unit tests + production build                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | The gate's test verb now tests logic, not just that the build succeeds.                                                                                                                                                                                                                                                                                                          |
| A38 | **Auto-linking + Mentions** — body prose links other terms at their first mention per page (longest name wins; EN/DA inflections; English loanwords recognised on Danish pages). Mentions are derived from prose, shown as "Mentioned in" (dashed, weaker) when not otherwise related, and feed lint W6.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | SPEC §4 ("Terms auto-link") and §6 (Mentions: derived, never authored).                                                                                                                                                                                                                                                                                                          |
| A39 | **Search intents** — "X vs Y" → compare page (or the route if they aren't a contrast pair), "how are X and Y related" / "from X to Y" / "X -> Y" → Explorer route, "before X" / "før X" → the term's "What to learn first". Phrases resolve to the best-matching term.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | The product spec's search architecture (compare, path and prerequisite intents), without a server.                                                                                                                                                                                                                                                                               |
| A40 | **Review queue** (`/[lang]/review/`) lists every draft by cluster with an "Edit on GitHub" link; footer links it                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | 150 AI-drafted entries need a human pass; this makes the W5 draft ratio actionable.                                                                                                                                                                                                                                                                                              |
| A41 | **Era view built** (supersedes A28): `/[lang]/timeline/` lists dated terms by decade (static HTML, cluster colour, links to each entry); the Explorer gains a 2D **Time** layout (x = `era`, same-year terms stacked, odd years staggered half a row so labels don't collide). Terms without an `era` are omitted from the Timeline with a count, and parked in a grid lane left of the earliest year in the Explorer.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | Eras are now being authored (PR #7); a missing year is shown as missing, never guessed.                                                                                                                                                                                                                                                                                          |
| A42 | **`era` flows through the derived graph** (`GraphNode.era`, so `/graph.json`), with pure helpers in `src/lib/era.ts` (`decadeOf`, `groupByDecade`, `timePositions`) shared by page and Explorer and unit-tested                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | One implementation (A27); the page and the island can't disagree about ordering.                                                                                                                                                                                                                                                                                                 |
| A43 | **Timeline domain filter is CSS-only** (radio chips + generated `:has()` rules that hide terms outside the domain and decades left empty); 3D stays on Depth only                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | No island for a filter; the Timeline ships zero JavaScript.                                                                                                                                                                                                                                                                                                                      |
| A44 | **Optional accounts + synced progress via Supabase (free tier), called from the browser** — the site stays static on GitHub Pages. Enabled only when the build gets `PUBLIC_SUPABASE_URL` + `PUBLIC_SUPABASE_ANON_KEY` (deploy reads repo `vars.*`); without them no account UI is rendered, no Supabase code loads, and the gate/CI need no secrets. When enabled, a small header island (sign-in / account link + sync dot) runs on every page, so the Timeline is no longer zero-JS in that build (A43 holds for unconfigured builds). Setup: `docs/SUPABASE_SETUP.md`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | Accounts without running a server: hosted Auth + Postgres with Row Level Security, and the public URL/key are safe to ship. Absent config = today's site exactly.                                                                                                                                                                                                                |
| A45 | **Learner state + sync merge**: each term carries `reviewed` (last answer) and `statusAt` (last status set/clear) timestamps under the single key `atlas:learner:v2`. Remote = one `learner_state` row per user (`jsonb`, RLS own-row for all four verbs, 512 KB cap, server-set `updated_at`/`version`). Merge (pure, `src/lib/sync.ts`, commutative + idempotent, unit-tested): schedule from the most recent answer (tie → higher box), status from the most recent change (a clear wins if newer), right/wrong = max. Sync = pull → merge → save locally → versioned write if different; on sign-in, load, tab visible, back online, and 1.5 s after a local change.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | Local-first stays true: localStorage is what the UI reads. Most-recent-answer (not highest box) lets a wrong answer on one device demote; max counts avoid double-counting shared history.                                                                                                                                                                                       |
| A46 | *(Email hidden and LinkedIn added per A87.)* **Auth: email magic link + GitHub OAuth, PKCE flow**, returning to `/[lang]/account/`; auth errors handed back in the redirect URL are shown there. Plain "Sign out" signs out this browser only.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | PKCE keeps tokens out of the URL; the trade-off is that a sign-in link must be opened in the browser that asked for it (the page says so).                                                                                                                                                                                                                                       |
| A47 | **"Delete my synced data" writes a tombstone**: the row's `state` becomes `{}` and `deleted_at` is set (server time); a check constraint keeps `state` empty while `deleted_at` is set, so no device can write progress back. Then every session is revoked (`signOut` global; a failure is shown, and this browser is signed out locally anyway). A device signed in _before_ the delete may keep a valid access token for up to an hour: on its next sync it finds the tombstone, signs itself out and shows a notice. A session started _after_ the delete stays signed in with syncing paused until the learner chooses "Start syncing again", which clears the tombstone with this browser's progress. Progress in each browser is kept. The auth identity stays in Supabase until the owner deletes the user (cascades to the row). Known limitation: signing into a _different_ account on the same browser merges that browser's local progress into it.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | A plain row delete could be undone by any still-signed-in device's next sync. Deleting an auth user needs the service key, which must not ship to the browser. Per-user local partitions aren't worth it for a personal study tool.                                                                                                                                              |
| A48 | **Change timestamps are monotonic per term** (`max(now, previous + 1)`), and synced timestamps more than a day ahead of this clock are pulled back to now when parsed                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | A device with a fast clock could otherwise make its changes unbeatable; a local change made after merging always counts as newer.                                                                                                                                                                                                                                                |
| A49 | **Optimistic concurrency on the row**: the server bumps `version` on every write; a client updates only `.eq('version', seen)` (or inserts when no row exists), and on 0 rows changed / duplicate insert it re-pulls, re-merges and retries (up to 4 times)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | Pull-then-upsert could overwrite another device's write that landed in between; with a version check no write is silently lost.                                                                                                                                                                                                                                                  |
| A50 | **No backwards-compatibility work for learner state** — the site has no real users yet, so there is no migration from the pre-sync `atlas:learner:v1` data; one key, one format                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | Keeps the code free of legacy shims; any old local progress is simply not read.                                                                                                                                                                                                                                                                                                  |
| A51 | *(Superseded by A75–A78.)* **Semantic search, fully static**: terms are embedded (name + aliases + summary + plain facet, one vector per language) with `Xenova/multilingual-e5-small` **pinned to Hugging Face commit `761b726d…`** (q8, e5 `passage:`/`query:` prefixes, mean pooling + normalisation — the same options at embed and query time) via `@huggingface/transformers`; the browser embeds the query with the same pinned model in a web worker (transformers.js 4.3 still reads `config.json` and sends 1-byte Range probes at `main` to plan files and progress totals; config, tokenizer and weights themselves are loaded at the pinned revision) and ranks terms by cosine, each term scoring its better language (so Danish queries find English-named terms). Pure helpers in `src/lib/semantic.ts`, unit-tested. | The user calls semantic search vital; no server, no paid API, no database (SPEC §10). One small multilingual model covers EN, DA and cross-language; pinning the revision guarantees query and term vectors come from identical weights. |
| A52 | *(Superseded by A75–A78.)* **Vectors are committed, not built in CI**: `npm run embed` writes `app/public/semantic/vectors.json` (Int8, one scale per vector, base64 — ~280 KB for 255 terms) with a settings hash (model + revision + dtype + prefixes + embed options + quantisation version + languages) and a short hash of each term's passages. The lint errors (**E11**) only when search would be wrong — other settings, or a term without vectors / vectors for a removed term — and **warns (W8)**, listing them, when an existing term's text changed since it was embedded. The same run embeds 16 fixture queries (`src/lib/semantic.fixture.json`) so Vitest checks real EN, DA and cross-language rankings offline. | Downloading a 118 MB model on every CI run is slow and a flaky external dependency. A per-term check keeps contributors unblocked: rewording a summary leaves a slightly stale vector (still a good match), which the warning surfaces until someone runs `npm run embed`; adding or removing a term does block, since that term would be missing from, or dangling in, results. |
| A53 | *(Superseded by A75–A78.)* **The model loads only on demand, and the first download is opt-in**: a query of three or more words, or one with no lexical match (only once the index has loaded), shows "Search by meaning (downloads a ~135 MB language model from Hugging Face the first time)" (tooltip names huggingface.co). Opting in asks for persistent storage (`navigator.storage.persist()`); after a successful load a `localStorage` flag makes such queries run semantically automatically — but only if the Cache API still holds the model file; if it was evicted the opt-in is shown again. A "Turn off search by meaning" control clears the flag. A failure (load, query or worker crash) is shown with **Try again** and never retried automatically; the worker forgets a failed load, not a failed query. | The only multilingual models small enough are still ~118 MB, plus a 17 MB tokenizer (the 250k-token vocabulary dominates; q4/fp16 variants are larger). Starting or restarting that silently — on a phone, after cache eviction, or in a retry loop — would be hostile; the user decides every download. |
| A54 | *(Carried into A78; margin re-tuned in A76.)* **Hybrid ranking = reciprocal rank fusion (k = 60)** of the semantic hits and a lexical search over **names and aliases only**, with English + Danish function words dropped; semantic hits are kept only within 0.03 cosine of the best one; hits found only by meaning carry a "by meaning" label. Natural-language queries also drop function words in the plain lexical search. | Rank fusion avoids reconciling MiniSearch and cosine scales. Summary words ("stopping", "people") and prefix matches ("do" → Plan-Do-Check-Act) were pure noise for questions, while a named term ("MFA") is a strong signal. e5 cosines sit in a narrow 0.8–0.9 band, so a margin below the top beats an absolute threshold. |
| A55 | *(Superseded by A75–A78.)* **Runtime hosts**: the model comes from the Hugging Face Hub at the pinned revision; the ONNX runtime (`ort-wasm-simd-threaded.asyncify` `.wasm` + `.mjs`) is **self-hosted** — imported with Vite `?url` and set as `env.backends.onnx.wasm.wasmPaths`, so no code comes from a third-party CDN. `onnxruntime-web` is a direct dependency pinned to exactly the version `@huggingface/transformers@4.3.0` requires, **`1.31.0-dev.20260914-8d85527a0` — a dev prerelease**, which transformers.js pins; revisit when it moves to a stable release. CI installs with `ONNXRUNTIME_NODE_INSTALL=skip` so `npm ci` doesn't pull the ~GB CUDA binaries `onnxruntime-node` fetches on linux-x64 (only `npm run embed` uses it, on CPU). | GitHub Pages can't host a 118 MB file (100 MB limit), so the pinned model stays on Hugging Face; the runtime (27 MB wasm) fits, and serving it ourselves removes a CDN from the code path. The asyncify build is used everywhere (transformers.js's CDN default picks it too, except for Safari < 26 without WebGPU, which it sends a non-asyncify build). |
| A56 | **Disambiguation page at `/[lang]/terms/<name>/`** (ADR-0003's `/term/token`, translated to the built URL scheme `/[lang]/terms/<folder>/<id>/`): generated for every bare id name held by two or more folders (today only `audit`), listing each meaning with its domain, cluster and summary. A Collision is defined by the **shared bare id** (`cs/audit` + `security/audit`), not by display names (`cs/audit` is shown as "Audit logging"), matching E3 and the graph's `collides`. Linked from each colliding term's page, and from search when the query equals the shared name (case-, space- and hyphen-insensitive). Pure helpers in `src/lib/collisions.ts`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | The ADR fixes the behaviour (the bare name serves a generated page); the URL keeps the existing `terms/` prefix so the bare name sits exactly one folder level above its meanings. A partial match ("aud") or a fuller name ("audit logging") already finds the right term, so only the exact shared name is ambiguous.                                                          |
| A57 | **W2 redundant child, read literally**: a term's resolved authored edges (as type + target), excluding its own `kind-of` edge to that parent, are a subset of the parent's authored edges, **and** its summary contains the parent's display name (whole word, case-insensitive, either language; "Intrusion detection system (IDS)" also matches "IDS"). Aliases and generated inverses are not counted.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | 02_SCHEMA §6 / ADR-0002: "Edges are a subset of its `kind-of` parent's and its summary contains the parent's name." The child's link to the parent can never be in the parent's own edges, so counting it would make the rule unreachable.                                                                                                                                       |
| A58 | **E5 circular definition, read literally**: a term's definition = its summary + the four body facets (English, the blocking language — ADR-0009); the terms it names are its auto-link Mentions (A38). A term is **grounded** when its "In plain English" facet names no other term. E5 fails on every loop (strongly connected set of ≥ 2 terms) that contains no grounded term. The first run found one loop — `cs/internet`, `cs/port`, `cs/tcp-ip` — caused by "postal service(s)" in two plain facets auto-linking to `cs/service`; the content was fixed ("post office(s)"), not the rule, and both terms re-embedded.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | "No set of Terms defines each other with no plain-English grounding anywhere in the loop" — the `plain` facet is the schema's plain-English grounding (SPEC §4: it "breaks circular terminology"). Reusing Mentions keeps one notion of "names a term". The rephrase also removes a visible mislink on both pages.                                                               |
| A59 | **W3 "non-foundational" = no other term `requires` it.** W3 warns on a term that requires nothing and that nothing requires — it sits outside the prerequisite graph entirely.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | The spec never defines "foundational". Depth 0 cannot be the test (every term with empty `requires` is depth 0, so W3 would never fire); a term others build on is foundational by definition.                                                                                                                                                                                   |
| A60 | **Build reports are printed by the content lint**, which runs first in every `npm run build` (and in the gate): draft ratio **per domain** (drafts are per term, so there is no per-language split), coverage with clusters under ten, the **depth histogram** (terms per `requires` depth) and the **collision list** (every name serving a Disambiguation page). Pure helpers in `src/lib/lint-rules.ts`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | SPEC §5 "Reports (every build)"; the lint already printed coverage and the overall draft ratio, so every report lives in one place.                                                                                                                                                                                                                                              |
| A61 | **Guided tour = a hand-rolled Preact island, no dependency.** *(Motion, placement and page changes superseded by A85; steps trimmed to 13.)* `Tour.tsx` is mounted once in `Base.astro`; steps (`TOUR_STEPS`, bilingual) live in `site.ts`, pure logic in `src/lib/tour.ts`. A step names a page relative to the language root and a list of anchor selectors, tried in order, falling back to a centred card (anchors inside client-only islands such as the Explorer are awaited ≤ 3 s). Position is kept in `localStorage` (`atlas.tour` = `{active, step}`), so the tour follows the reader across pages and languages; on another page it offers a "Continue the tour" pill instead of taking over. A first visit on **any** page shows a welcome card (Start / Not now / "Don't show again"); "Not now" and Esc snooze it for the session (`sessionStorage`), finishing or "don't show again" set `atlas.tour.done`. **Without storage** (probed on load) the tour never auto-starts — it could not remember a dismissal — and carries its step across page loads in the URL (`?tour=N`, removed again with `history.replaceState`), so "Take the tour" still works. Closing returns focus to the "Take the tour" control that opened it, else to `#main`; Esc is left to a text field or an open mobile menu first. The card is a **non-modal** dialog (`role="dialog"`, `aria-modal="false"`, focus moved to it on each step, Esc closes) so the reader can try the thing it points at; on phones it is a bottom sheet. It walks `security/risk` and `risk-vs-threat` — a GRC staple whose page has every section (learn-first, relationships, mentions, quiz); a unit test pins that the term and pair exist. The Explorer gets only three `data-tour` attributes, with `#explorer aside` as fallback, so the graph overhaul can move markup freely. | Libraries (driver.js, shepherd) don't resume across full page loads, which a static multi-page site needs; the whole thing is ~300 lines and adds nothing to `npm audit`. Auto-starting by teleporting a deep-linked visitor to the home page would be hostile, so step 0 is page-agnostic and "Start" is what navigates.                                                        |
| A62 | **Home page = hero + search + domain entry points + the full index.** Hero text, the search box (with a "/" hint), four domain cards with term/cluster counts linking to anchors in the index below, a random-term link (works without JS: first term), links to Explorer/Study, and **Recently viewed** (up to 8 ids in `localStorage` `atlas.recent`, written by term pages). The A–Z index stays on the page under "All terms" — "Index" in the nav still means it. Term pages get a breadcrumb (Index › primary domain › cluster, linking into those anchors).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | The page is both landing and index; splitting them would add a route for no gain. The breadcrumb uses the folder (primary) domain because that is the one the index groups by.                                                                                                                                                                                                   |
| A63 | **`/` still redirects to `/en/`** (owner undecided). Instead, an English page shows a Danish banner (not on full-width pages — it would push the Explorer's fixed-height graph off screen) — "Denne side findes på dansk. Skift til dansk / Nej tak" — when `navigator.languages` ranks Danish above English (`da`, `da-*`); "Nej tak" is remembered in `localStorage`. No reverse banner on Danish pages.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | Suggest, don't redirect: an automatic switch would fight the owner's open decision and trap readers who want the English terms.                                                                                                                                                                                                                                                  |
| A64 | **UX baseline**: mobile nav (hamburger ≤ md, `aria-expanded`, Esc closes; `<noscript>` shows it), header height unchanged (the Explorer sizes itself to it); "/" focuses search or opens the home page's (`#search`); skip-to-content link; global amber `:focus-visible` outline; back-to-top on long pages; copy-link on term and compare pages; `404.astro` (bilingual, no tour auto-start) — GitHub Pages serves `dist/404.html`; canonical + Open Graph/Twitter `summary` meta (no `og:image` — OG does not render SVG, and there is no raster art yet); print styles (white page, no chrome, quiz or graph); `prefers-reduced-motion`. Search shows "Loading…" while its index loads, and an intent phrase that **exactly** names a term (or alias) resolves to it before fuzzy ranking ("risk vs threat" had resolved "threat" to Threat hunting). **No light theme**: colours are hard-coded `neutral-*` utilities on every page, so a toggle is not cheap; revisit with design tokens.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | Clear wins from a UX audit, each small and independent of the graph rendering being reworked elsewhere.                                                                                                                                                                                                                                                                          |

| A65 | **Deploy only what passed the gate**: `deploy.yml` runs on `workflow_run` of `gate` (push to `main`, conclusion `success`) and checks out that run's `head_sha`; `workflow_dispatch` stays for manual redeploys. A gate run cancelled by a newer push (`cancel-in-progress`) deploys nothing; the newer run deploys. Both workflows check out full history (`fetch-depth: 0`) for the feeds (A71). | DECISIONS_REVIEW U28: a red gate on `main` could still deploy. `workflow_run` keeps one gate run per push (a reusable-workflow call would run the gate twice) and deploys exactly the gated commit. |
| A66 | **Content licence = CC BY-SA 4.0** (`CONTENT-LICENSE.md`, legal text `LICENSE-CONTENT.txt`): term files, articles, allowed-words lists and every published data file. Code stays MIT (A3). Exports carry the licence inside; the site footer and the Data page link it; term pages declare it in their structured data. | DECISIONS_REVIEW A3/U16: MIT covered the content by default while the Closed Vocabulary wordlists in the same repo are CC BY-SA 4.0. ShareAlike matches them and Wikipedia and keeps derived dictionaries open. **Not settled:** whether compendium-seeded Danish wording needs the course owner's permission (U16) — the owner's call. |
| A67 | **Dependabot**, weekly (Monday): npm in `app/` with every minor + patch bump grouped into one PR (majors one per package), and GitHub Actions grouped into one PR. `onnxruntime-web` and `@huggingface/transformers` are ignored. | DECISIONS_REVIEW U10/F18: nothing bumped dependencies or the SHA-pinned actions. `onnxruntime-web` is pinned to exactly the prerelease `@huggingface/transformers` requires (A55), and the self-hosted runtime must match; a bump of either alone would break semantic search in a way the gate (no browser tests) cannot see, so the pair moves together by hand. Dependabot rather than Renovate: built into GitHub, no app to install. |
| A68 | **SEO basics**, extending A64's canonical + Open Graph meta (one set of head tags in `Base.astro`): `hreflang` `x-default` (English), a `noindex` prop that also drops the canonical link and `og:url` (the 404), a `<link rel="sitemap">` and its language's RSS link; term pages add schema.org `DefinedTerm` JSON-LD (in the `Atlas` `DefinedTermSet`, with the licence). The sitemap (`sitemap-index.xml` → `sitemap-0.xml`, with `hreflang` alternates) is written by a 70-line local integration (`app/integrations/sitemap.mjs`) from the HTML actually emitted, skipping `noindex` pages, meta-refresh redirects, the 404 and the review queue. `robots.txt` ships but only takes effect at a host root (custom domain, F4). **Drafts are not `noindex`ed.** | DECISIONS_REVIEW U33 (SEO minimal; SEO was an ADR-0008 argument). A local integration instead of `@astrojs/sitemap` avoids two dependencies (zod 4, `sitemap`) and can never list a route that wasn't built. `noindex` on drafts would deindex every term (all are drafts); that is an owner decision (U33), not a default. |
| A69 | **Open data**: `/api/terms.json` (every term: authored fields, edges resolved to full ids, derived depth, absolute URLs, the licence), `/api/terms/<folder>/<id>.json` per term, `/api/terms.csv` (one row per term, both languages, UTF-8 BOM, CRLF), and `/api/anki-en.txt` / `/api/anki-da.txt` in Anki's text-import format (headers set separator, HTML, note type, deck, tags and a stable GUID column, so re-importing updates notes). `/[lang]/data/` lists them with `/graph.json` and the feeds. Pure builders in `src/lib/export.ts`, unit-tested. Term pages link "This term as JSON". | DECISIONS_REVIEW F15 (publish the data once the licence is set — A66). A real `.apkg` needs SQLite in the build; Anki's documented text import gives the same deck with no dependency. Inverse edges are left out of the exports: they are derivable and would double every edge. |
| A70 | **Doc refresh**: `design/schema.ts` now re-exports `app/src/schema.ts` instead of being a drifted copy; SPEC §3 (collisions as built, ai/platform written under P1), §5 (Danish advisory, matching ADR-0009), §7 (open data, A–Z, Continue learning), §11, §13 (name resolved as A1), §14 (ADRs 0001–0009, D1–D13) corrected; ADR-0003/0004 get implementation notes (YAML terms, article paths); AGENTS.md test verb; `app/README.md` stack. The decisions first stated only in PR bodies are recorded below as **P1–P9** (the ids `DECISIONS_REVIEW.md` uses). | DECISIONS_REVIEW U37: agents are told "the spec wins", and the spec contradicted itself. Re-exporting keeps every existing link to `design/schema.ts` true. Scope changes (P1) are recorded as they happened, not approved. |
| A71 | **RSS feed of new terms per language** (`/en/feed.xml`, `/da/feed.xml`, the 50 newest). A term's date is the author date of the commit that first added its file (`git log --diff-filter=A`, one call per build). In a shallow clone or without git the feed is built with no items and a warning, never with wrong dates. | The repo has no authored "added" date, and adding one would touch every term file. Git history is the only honest source. A feed of terms (not of edits) is what a learner can use. |
| A72 | **"Just works" URLs**: the bilingual 404 from A64 (one page) is now `noindex` and also links the A–Z index in both languages; `/[lang]/terms/` is an A–Z index (letters in the page language, so Danish ends Æ Ø Å), linked from the home page and the 404; every **short name** (a bare id, a display name or an alias in either language, slugified with æ→ae, ø→oe, å→aa) that names exactly one term gets a redirect page at `/[lang]/terms/<name>/` (meta refresh + canonical + `noindex`), e.g. `/en/terms/mfa/`, `/da/terms/2fa/`. Collisions keep their Disambiguation page; names shared by two terms or equal to a folder get no redirect. The root `/` redirect now has a favicon and `noindex`. | SPEC §4 promises aliases "redirect here"; ADR-0003 planned bare-name URLs. The live crawl (2026-09-25) found GitHub's generic 404 (with links outside the site) and a `/favicon.ico` 404 on the root redirect. Pages has no server redirects, so redirect pages are the only mechanism; ambiguous names go to search, not a guess. |
| A73 | **Continue learning + print**: the sources block on a term page becomes "Sources · Continue learning", grouped by source tier, best first (standards › official docs › course material › reference › textbooks › other), with the publisher. Print extends A64's single print block (chrome, graph and quiz are `print:hidden` in the markup): islands never print, the main column uses the full page width, and outside links print their address. | SPEC §7: "_Continue learning_ (external resources by type)" was never built, and the schema has no resources field; the sources already carry a tier (D11), so grouping them delivers it without a schema change. Printing for study is common for the course audience. A share button was left out: not in the SPEC, and it is UI work owned by the UI/UX stream. |
| A74 | **Graph visual language lives in `src/lib/graph-style.ts`** (pure, unit-tested) with Cytoscape glue in `src/lib/graph-cytoscape.ts`, shared by the Explorer and the term-page graph. **Colour**: each domain owns a hue (security 355°, cs 212°, ai 285°, platform 150°; ≥ 60° apart); an unlisted future domain gets a stable hue hashed from its name and stepped by the golden angle until it is ≥ 34° from every listed one. A cluster is a shade of its domain's hue (±22°, three lightness steps), via a static `CLUSTER_DOMAIN` map beside `CLUSTER_LABELS` (a test keeps the two in step); an unlisted cluster takes its domain's base colour. A term in two domains wears a **ring** (2D border; 3D translucent shell) in the other domain's colour. `CLUSTER_COLOURS`/`FAMILY_COLOURS` are re-exported from `site.ts`, so the Timeline follows. **Edges**: colour = relationship family (A29); **direction** comes from the schema's `symmetric` flag (a test pins it) — every non-symmetric type (incl. kind-of/part-of) gets an arrow and flow, contrasts/alternatives/used-with get neither; an edge whose endpoints share **no domain** is drawn as a gradient source-domain → family → target-domain colour. The term page draws edges in their **authored** direction (a generated inverse "unlocks" is drawn as the authored "requires" pointing at the term), so arrows mean the same thing everywhere. **Flow** = marching dashes (`line-dash-offset`, ~30 fps, 18 px/s) only where cheap: every one-way edge when a view has ≤ 120 of them, otherwise only the hovered neighbourhood, the selected term's edges and a highlighted route; the loop idles when nothing flows, wakes on class changes, and follows `prefers-reduced-motion` live (nothing moves while it is set). **Curves**: `unbundled-bezier`, parallel edges fanned on alternating sides (3D: `linkCurvature` from the same offsets). **Island layout** (whole map, ≥ 60 terms): each cluster is laid out alone with `cytoscape-fcose` from its own edges only (seeded `Math.random`, so deterministic), becoming an island; `packIslands` then packs each domain's islands into a region (45 px gaps, drawn together by the edges between them) and the regions apart (150 px gaps) — cross-cluster edges take no part in placing terms and are drawn faint (10 % opacity). Measured in headless Edge: ≈ 0.17 s for the 315-term map. Cluster names sit just above their island with a halo; domain names outside their region, on the side facing away from the map's centre. A neighbourhood view (< 60 terms) uses one fcose run. A single fcose pass over the whole map (with seeded cluster/domain start positions and per-edge ideal lengths), and compound "hull" nodes per cluster, were both tried and dropped: clusters kept overlapping. **Labels never overlap**: a greedy cull keeps labels in order of importance (node size, i.e. degree) and hides any whose box would touch one already kept or a cluster name; boxes are computed from data (position, size, font, canvas-measured text), not read back from Cytoscape, whose cached boxes lag class changes. Cytoscape's own `min-zoomed-font-size` is off for terms — the cull also hides labels smaller than 8 px on screen — and it re-runs only when the zoom changes which labels are drawn. Hidden labels show on hover; a hovered neighbourhood's labels are culled among themselves (hovered term first) and drawn at a readable size when zoomed out. `cytoscape-fcose` (MIT) sits in the Explorer chunk only; the term page uses built-in `concentric` (focal term centred, neighbours grouped by family, labels pointing outward). **3D**: same colours, curved links, directional particles on one-way links (constant counts; faded links take the background colour, so hover never rebuilds them), a cluster pull in the horizontal plane (height stays Depth), accessors set once and re-evaluated on hover/selection, additive glow sprites instead of bloom post-processing (UnrealBloomPass washed the background grey), `three` as a direct dependency at the version 3d-force-graph already resolves. | The owner asked for domain colour families, directed/animated edges, curves, a clustered layout that reads as separate systems, a legend and glow; none of it may cost correctness or the reduced-motion preference. Laying clusters out separately guarantees the islands a force layout only approximates. Deriving direction from the schema keeps one source of truth; per-frame restyling of ~800 edges would cost battery for little meaning, so flow is reserved for what the reader is looking at. |
| A75 | **Search by meaning runs on the backend** (supersedes A51, A53, A55): term vectors live in the project's Supabase Postgres (`public.term_vectors`, pgvector `vector(1024)`, one row per term and language, RLS public read-only, written only by the service role) with a `match_terms(query_embedding, match_count)` SQL function (cosine; each term scores its better language); a Supabase Edge Function, **`semantic-search`**, embeds the query and calls it. The model is **bge-m3 (`@cf/baai/bge-m3`) on Cloudflare Workers AI**, called from the Edge Function with a Workers AI token held as a Supabase function secret. Nothing model-related reaches the browser: `@huggingface/transformers` is a devDependency of `npm run embed` only; `onnxruntime-web`, the self-hosted wasm and the worker are gone. Deployed by `.github/workflows/backend.yml` (below). | The owner rejected a ~135 MB in-browser download. **A model inside the Edge Function does not fit** (checked 2026-09-25): Supabase's limits are 256 MB memory, **2 s CPU per request**, 20 MB bundle, and an isolate that reaches 50 % of any limit is shut down after its request. Loading the smallest multilingual model, `multilingual-e5-small` q8 (118 MB weights + 17 MB tokenizer), in Node with the *native* ONNX runtime measured **+360 MB RSS (532 MB total) and 1.8 s CPU warm / 3.0 s cold** before the first query — the Deno wasm runtime would only be heavier; a user reports only models up to 384 dimensions working there, and settling on `gte-small` (supabase discussion #34688). The only built-in model (`Supabase.ai.Session`, `gte-small`) is **English-only** per Supabase's docs. So the fallback the brief allows: Workers AI's bge-m3 is multilingual (Danish included), 1024-dim, and costs ~1,075 neurons per million tokens against a free 10,000 neurons/day — a 20-token query is ~0.02 neurons. Keeping pgvector + the function in Supabase leaves one backend URL, one CORS/rate-limit point, and the vectors next to the database; only the embedding is computed at Cloudflare. |
| A76 | **Stored and query vectors come from one service, by construction** (supersedes A52): the database is **never seeded from a file** — `npm run seed:vectors` (run by CI) re-embeds every term's passages through Workers AI with the **same `cloudflareEmbed` call the function uses for queries**, upserts every (term, language) row and deletes rows of removed terms; it refuses to run without Cloudflare credentials, or when the committed vector file is out of date. That file, `supabase/seed/term-vectors.json` (written by `npm run embed`; outside the site; Int8 + one scale per vector, base64, ~1.2 MB for 419 terms × 2 languages, plus the 16 fixture query vectors), is kept only as the lint's per-term hash source (E11/W8, A52's semantics) and the offline ranking tests' index. `npm run embed` uses Workers AI when `CLOUDFLARE_*` are set, else the full-precision ONNX export of the same weights (`Xenova/bge-m3`, pinned `4de13258…`, [CLS] + normalisation) locally; the committed copy was made the second way (its `backend` field says so). The pooling Workers AI reports (undocumented for bge-m3) is **logged, not asserted**. After seeding, a **smoke test** (`npm run smoke:semantic`, `SMOKE_QUERIES`: one Danish, two English questions, each also an offline fixture expectation) fails the workflow unless each finds its term in the deployed function's top 3. `nearBest`'s margin is re-tuned to **0.06** for bge-m3 (fixture answers sit within 0.03 of the top hit; top scores 0.59–0.71). | Review of PR #18: local ONNX [CLS] vectors against Workers AI query vectors with an undocumented pooling could silently mismatch. Embedding both sides through one call removes the question instead of testing for it, and the smoke test catches anything else that breaks ranking end to end. The committed copy still keeps the lint and the offline tests free of network and model downloads. |
| A77 | **Function contract and abuse limits**: `POST { q, lang, k? }` → `{ hits: [{ id, score }] }`; `q` 1–200 characters, `lang` `en`/`da` (validated; ranking always uses both languages), `k` 1–20 (default 8); the body is read with a **2 KB cap counted while streaming** (413 beyond), then parsed. **Rate limits in Postgres**, shared by every isolate: `public.search_allow(client)` (`security definer`, `search_path = ''`, executable by the service role only) counts in `private.search_rate` (a schema the Data API doesn't expose) **30 requests per client per minute**, then **50,000 per day in total** → 429; the client key is a SHA-256 of the IP (the table never holds addresses), the IP is `cf-connecting-ip`, else the **rightmost** X-Forwarded-For hop (the leftmost is client-supplied). A per-isolate limiter in front only spares the database an obvious flood. **One 5.5 s deadline** covers the rate check, Workers AI and `match_terms` together (the browser gives up at 6 s; raised from 1.7 s / 2 s on 2026-09-25 by the owner because cold starts failed the first query). CORS echoes only `https://cmaintz.github.io` and `http://localhost`/`127.0.0.1`. Deployed with `--no-verify-jwt`; `match_terms` runs as anon (RLS applies), `search_allow` with the service role — both **legacy API keys**, injected by the platform, so they must stay enabled. Missing configuration → 503, upstream failure or deadline → 502. Pure parts (validation, CORS, IP, body cap, Workers AI client, row parsing) live in `supabase/functions/semantic-search/logic.ts`, unit-tested by the app's Vitest and shared with the seed script. | Anonymous search is the product; a JWT gate would only stop honest clients. **The binding budget is Supabase Edge Function invocations — 500,000 a month on the free plan** (~16,000 a day), not Workers AI (10,000 neurons/day ≈ hundreds of thousands of short queries). The daily cap stops one abuser from spending more than a few days' worth of that budget in a day; the per-client limit keeps one client from exhausting the daily cap. Rejected requests still count as invocations — nothing inside the function can prevent that; only an edge WAF could. Keying by rightmost hop / `cf-connecting-ip` stops the trivial bypass of sending a fresh fake first hop per request. |
| A78 | **Client: opt-in by configuration, no model, silent fallback**: search by meaning is on only when the build gets **`PUBLIC_SEMANTIC_SEARCH_URL`** (the function's URL; repo variable in `deploy.yml`) — independent of `PUBLIC_SUPABASE_URL`, so a build with only sync configured never calls an undeployed function (404 noise). For natural-language queries (A54's rule) the search box calls it 300 ms after the last keystroke, aborting the previous request, with a **2 s timeout**; answers are cached per visit. On no URL, an error or a timeout it shows the lexical results as always — no message, no retry button. RRF, the names-and-aliases lexical side, stop-words and the "by meaning" label carry over from A54. **`.github/workflows/backend.yml`** runs like `deploy.yml` (A65): on `workflow_run` of a successful `gate` on a push to `main`, checking out exactly that `head_sha`; manual runs only on `main`; 20-minute timeout. It is skipped unless repo var `SUPABASE_PROJECT_REF` is set; steps are skipped with a notice unless secrets `SUPABASE_ACCESS_TOKEN` + `SUPABASE_DB_PASSWORD` are set (migrations only), and without `CLOUDFLARE_ACCOUNT_ID` + `CLOUDFLARE_API_TOKEN` nothing past the migrations runs. Then `supabase db push`, `supabase secrets set`, `supabase functions deploy`, `npm run seed:vectors` (service key read via the CLI, masked), `npm run smoke:semantic`. Supabase CLI pinned to 2.117.0, actions SHA-pinned. | Search must never feel broken because an optional backend is down or paused (free Supabase projects pause after a week idle); the lexical results are already good for names. Deploying the backend only from gated `main` commits matches the site's deploy rule. |
| A79 | **Quizzes never ask a term page's own answer; sync is invisible**: (1) questions split into two families — *about* X (relationships in both directions, odd-one-out, true/false) whose answer is always another term, and *answered by* X (definition → term, term → definition). A term page shows the first family plus definition questions for X's neighbours, so a question answered by X appears on its neighbours' pages and in study sessions, never on X's. True/false is false only by reversing a `requires` / `kind-of` / `part-of` / `implements` / `supersedes` edge. Wrong options: same role elsewhere in the map first, then cluster, then domain; never a neighbour of X, its kin (structure, alternative, contrast), a valid answer or one further along a transitive chain, or a term named in X's prose. (2) The study page's "Quiz me on" picks everything, a domain, a cluster or *my weak terms* (last answer wrong, or marked Learning / Don't understand); sessions rotate question kinds and keep due-first spaced repetition. (3) No "Sync now" button: the header shows ✓ / syncing… (only if it takes over 400 ms) / offline / not synced + "Try again"; a failed sync retries itself after 5 s, 15 s, 60 s, then every 5 min. (4) Ten `requires` edges added where a foundation was plainly missing: `oauth`, `conditional-access`, `cloud-iam` → `access-control`; `passkey`, `one-time-password` → `authentication`; `qualitative-risk-analysis`, `quantitative-risk-analysis`, `risk-identification`, `risk-transfer` → `risk`; `computer-use` → `ai-agent`. `kind-of` / `part-of` children were not also given `requires` on their parent. | Owner feedback: a question whose answer is the page you are reading tests nothing. Reversed edges are the only false statements the map can vouch for (the map is not complete, so "X protects against Z" without an edge may still be true). A button that must be pressed for something that already happens by itself reads as broken; silent retry covers the common case (a brief network or server hiccup). OIDC, SAML, SSO and federation already reach `authentication` through `identity-provider`, so they were left alone. |
| A80 | **Term panel in the Explorer; deep dive + sources on the term page.** Clicking (or double-clicking — it no longer navigates) a node opens `TermPanel.tsx`, docked right (26rem on ≥ lg, full width below) as an overlay on the map, not a flex sibling, so neither the Cytoscape nor the 3D canvas has to resize; the selected term is centred in the part of the map the panel leaves clear, and the fit/legend stack hides while it is open. Panel = `selected`: closing it (✕, Esc, empty-canvas click) clears the selection. It shows cluster/domain chips, aliases, summary, the four facets as tabs, an EN/DA toggle for the text (UI stays in the page language), *What to learn first*, relationships grouped in `RELATION_ORDER` (derived from `graph.json`, inverses via `EDGE_TYPES` passed as props so the island never bundles zod), self-assessment and a quiz on demand. Relationship/prerequisite chips (and nodes of the mini graph) re-select, never navigate. **Expand** grows the panel (width transition, off under reduced motion) over the whole Explorer below the header, lays the facets out 2×2 and adds the term-page neighbourhood graph (`Graph.tsx` gained an optional `onSelect`); expanded it is `role=dialog` with a Tab trap, docked `role=complementary`; Esc steps expanded → docked → closed; focus goes to the term name on each open and back to the opener on close. **Read more** links the term page. Only body/aliases/draft come from `/api/terms/<id>.json`; everything else is from the already-loaded graph, so a panel renders at once and the facets follow; records are cached per page and prefetched on node hover, chip hover/focus and for up to 12 neighbours of an open term. `?term=<id>` deep-links the panel (`history.replaceState`, other params kept; it wins over `?focus=` for the selection, `focus` still sets the 1-hop view). New optional schema field **`deepDive: { en, da }`** (plain text, blank-line paragraphs, exempt from Closed Vocabulary, auto-linked, included in the JSON export) renders as *Technical deep dive* after the facets; the sources block becomes **Sources & further reading** (still by tier, A73) with a **Where this data comes from** note (AI-drafted, draft or reviewed, review-queue, GitHub-edit and JSON links). `security/phishing` carries the one example deep dive. The Explorer's left-sidebar selection card is left as is (a parallel Explorer rewrite owns that file); the search box does not get the panel — it has no graph loaded and its results are links, so a panel there would be a second, graph-less variant. | The owner asked for a demo-style side panel that expands in place without a page load, with the full technical text and sources behind "Read more". Deriving the panel from the graph keeps it instant; the overlay keeps the Explorer diff small for the parallel rewrite. |
| A81 | **Timeline v2 — swim lanes** (supersedes A41’s decade list): `/[lang]/timeline/` is one lane per domain (security, cs, ai, platform; a term sits in its home domain's lane — the cluster's domain, as in A74 — or its first shown domain when that lane is filtered out) along a year axis with decade ticks, era bands behind (mainframes & time-sharing 1950–74, PCs & networks 1975–90, the web 1991–2005, cloud & mobile 2006–16, AI & agents 2017–; labels in `TIMELINE_UI`) and dots in the **domain colour** (ring = second domain), not cluster colour — the old cluster colours hid which domain a term belonged to. **Milestones** = the top 12 % of dated terms by degree plus each domain's biggest hub: larger, glowing dot and bold label. Desktop: horizontal, **density-weighted axis** (an empty year is 0.45 × the zoom's px/year, a year with terms grows with its busiest lane's count) so the empty 1950s–80s don't push the crowded 2010s–20s off-screen; labels stacked into rows by greedy first-fit interval packing (collision-free, minimal rows); horizontal scroll, sticky lane labels. Phones: vertical, lanes as columns, a stretch scale giving each year room for its busiest lane's stack (so nothing overlaps); names truncate until one domain is picked. Both layouts are server-rendered (CSS picks one), so the page reads without JavaScript; the Preact island (`client:idle`) adds domain toggles, zoom (5 steps) and a summary popover on hover/focus; **click/tap opens the Explorer's term panel (A80)** — its code (with the graph renderer) and `graph.json` are imported on the first click only, so the page stays a small island; until they arrive (or if they fail) the pinned popover stands in, and Escape/outside click closes it. The panel's page-rendered tables come from a shared `lib/panel-config.ts` (the Explorer page uses it too). Accessible fallback: a "List view" `<details>` with every dated term by decade, flowing in columns (top-down, not the old row-major zigzag). Pure geometry in `src/lib/timeline.ts` (unit-tested); the Explorer's Time layout (A41) is unchanged. *Update (owner feedback):* the page is now full-width and zoom 0 **fits the whole axis into the chart's width** (the density scale is linear in px/year, so one measurement solves it); each lane gets an equal share of the viewport's height and labels are placed hubs-first under that row budget, with what doesn't fit folded into "+N" chips (hover previews, click pins a list) so all four lanes fit on a 1440×900 screen; labels near the right edge flip left of their dot; decade gridlines are solid and labelled top and bottom over alternating decade bands, eras move to a strip above the axis; the legend's ring example is a real dot ringed in a second domain's colour. | Owner feedback: "the timeline is weird … visually it leaves a lot to be desired." |
| A82 | **Era backfill + the ordering report**: the Timeline's order was correct — decades ascending, years numeric, no sort, grouping or string/number bug (checked in the data and the live HTML). The CS lane *does* start after security and AI, and that is the data, not a defect: security's earliest term is PDCA (1950, a Deming management cycle the domain borrows), AI's is the name "artificial intelligence" (coined 1956), and the CS vocabulary in the corpus (password 1961, then operating system, database, protocol, kernel, network, API) entered common use in the 1960s. The old presentation made that look like an error: dots were coloured by **cluster**, so a CS term did not read as CS; the two-column grid read row-major, zigzagging the order; and 36 of 68 CS terms — the foundations — had no `era`, so CS looked thin until the 1970s–80s. The backfill moves CS's density into the 1960s but cannot honestly move its start before 1956 without inventing dates. Backfilled 50 `era` values where a date is defensible (year the idea entered common use; laws: adoption year): CS 14 (e.g. database 1964, operating system 1964 (OS/360), file system & process 1965 (Multics), protocol 1967, API 1968, kernel & network 1969, access control 1971, symmetric encryption 1977 (DES), certificate authority & SSO 1988, E2EE 1991 (PGP), conditional access 2016), AI 15, platform 16, security 5. Timeless or generic concepts (risk, asset, encryption, gradient descent, confusion matrix …) stay undated rather than get an invented year. No existing value was wrong enough to change (`hashing` stays 1979: its summary describes a cryptographic hash, Merkle's one-way hash, not Luhn's 1953 hash table). Coverage: AI 49 → 64/134, CS 32 → 46/68, platform 9 → 25/40, security 62 → 67/177; total 152 → 202/419 (undated 267 → 217). | Owner: "how come the earliest computer science term is younger than the youngest AI or cyber security term?" — answered with evidence, not a code change to ordering. |
| A83 | **Term panel: Previous/Next through connections, Back/Forward through viewed terms.** A slim bar under the panel toolbar walks the *anchor* term's connections in the order the relationships list shows them (group by group, `RELATION_ORDER`; a term listed under two types is two stops; terms not on the map are skipped), wrapping, with "3 of 12 · requires · ↩ anchor" text (the ↩ button returns to the anchor). The anchor stays fixed while stepping; any pick made another way (node, chip, mini-graph node, search, `?term=`) re-anchors on the picked term; Back/Forward keep the anchor if they land on it or one of its connections, else re-anchor. Back/Forward (← → in the toolbar) are a per-panel-session browser-style trail (capped at 50, forward trail dropped on a new visit); the trail is not the URL history. Keys while focus is in the panel, outside fields and widgets that consume arrows (the facet tabs): ←/→ previous/next connection, Alt+←/→ back/forward (preventDefault, so the browser does not navigate). Panel moves keep focus on the pressed control and announce "name — 3 of 12 · type" via an `aria-live` region; other picks still move focus to the term name. The panel tells its own moves from others by recording the id it asked `onSelect` for, so neither Explorer nor Timeline changed. Logic is pure in `term-panel.ts` (`connectionCycle`, `stepCycle`, `nextCycleState`, `visit`/`travel`). | The owner asked for next/previous in the panel "cycling through connected nodes or something"; "previous" also commonly means "the term I was just on", so both are offered. Anchoring the cycle lets a learner walk every connection and come back. |
| A84 | **About dialog** (Explorer corner "i" button + a footer link on every non-wide page): a native modal `<dialog>` island (`AboutButton.tsx`) modelled on the AI Coding Dictionary's about modal — eyebrow + title, two description paragraphs, GitHub/LinkedIn pills (new tab, `noopener noreferrer`), Credits (Christoffer Maintz, author) and Thanks (Christina Jakobsen, "Partner in crime" / "Makker i ugerningen"). Links live in `ABOUT_LINKS` (site.ts); one without a real http(s) URL is not rendered. Avatars read `app/public/about/<photo>` **only if the file exists at build time** (`about-assets.ts`, server-only), else an initials circle — no runtime 404; any aspect ratio is cropped to a centred circle (`object-fit: cover`, optional `photoPosition`). The Explorer's own top-right stack (Fit to view + legend) is pushed down by `--explorer-controls-top` from the explorer page's scoped style, because Explorer.tsx is being rewritten in parallel; the rewrite should read that variable and the selector bridge can then go. The button sits at z-10, under the term panel (z-20), like the Fit/legend stack. | Owner request (inspired by aicodingdictionary.com). |
| A85 | **Tour motion rework (supersedes A61's motion, placement and page changes; its structure, storage, restart, don't-show-again, `?tour=N` fallback and auto-start rules stand).** The owner found the tour laggy and jumpy; measured on the old build: `scrollIntoView` + `scrollBy` per step (jumps of up to 1.6k px), a re-render on every scroll event, a 200 ms `transition-all` on top/left/width/height, and CLS 0.2–0.4 on every page it landed on. Now: (1) each move **scrolls first** (one smooth `scrollBy`, only if the target and card don't already fit — pure `scrollDelta`), the card fading while the page moves, **then** the spotlight and card glide there together — one curve for everything, `cubic-bezier(0.22, 1, 0.36, 1)` (decelerating, no overshoot) over 420 ms, by `transform` (the spotlight also width/height); (2) placement is a pure `placeCard` — below, above, beside, else pinned to the bottom with the spotlight **clipped** above the card, so the card never covers its target (unit-tested); phones keep the bottom sheet, the spotlight clipped above it; (3) scroll/resize/reflow tracking reads once per animation frame and writes styles straight to the two elements — no Preact render per frame; (4) **before changing page** the tour shows a *bridge* card spotlighting the actual control that goes there (`via` on each step that starts a new page: the nav link, else a link in the page within 1.5 screens, else the logo; on phones the menu button, retargeting to the link once the menu opens) — the reader can follow it themselves (any click on a link to that page continues the tour) or press Next; with no such control nearby (home → the Risk entry) the tour goes straight there — a card pointing at nothing would only add a click. Back never bridges; (5) the overlay fades out before a page change and, on arrival, waits for fonts + idle, then fades in in place (no flight from the corner); closing fades out too; (6) `prefers-reduced-motion`: no transitions, instant scroll; (7) Esc is marked handled (`preventDefault`) for the Explorer's term panel; the card's own focus ring is a quiet grey (an amber ring read as a second spotlight); the tour sits above the BETA ribbon. Steps: “Search” + “Ask, don't just search” merged; “Mentioned in” dropped → 13 steps (plus the welcome card); the timeline step's text follows the swim-lane timeline (A81). The design demo has no tour at all (its onboarding is a one-line hint under the map and starter chips), so nothing was borrowed from it. | Fixes the feel without changing what the tour covers, which the owner said works. Cross-document View Transitions were tried for the page change but Chromium aborted them on every real page (“opt-in disabled”, reproduced in isolation), so they were left out rather than shipped half-working. |
| A86 | **Explorer v2: a light, stable overview** (supersedes A74's per-render layout, whole-map flow and 3D details). **Stable map**: `src/lib/explorer-2d.ts` builds one Cytoscape instance for the life of the page and `src/lib/explorer-3d.ts` one 3D scene (paused while hidden). Layouts are computed once from the *whole* graph. Relationship toggles fade edges in and out, and domain toggles hide terms in place — neither ever re-lays the map. **"Tidy up"** gathers the visible terms (islands re-packed, or lanes rebuilt), and switching force/depth/time glides the terms (700 ms; instant under reduced motion). Terms can't be dragged (`autoungrabify`, `enableNodeDrag(false)`). **Domain filter**: a term shows when *at least one* of its domains is enabled; a shared term is one node, and nothing appears merely because it links to a shown term. With its home domain off, a shared term takes an enabled domain's colour, and the disabled domain's names and legend rows disappear. **Overview = backbone**: each term keeps its 2 strongest in-cluster relationships (structure/prerequisite ×1.5); a term left with none keeps its single strongest. One faint gradient **ribbon per pair of related clusters**: width ∝ √count, hidden when zoomed in. Hover/selection reveals every edge of that term. The legend switch "Show all relationships" draws every edge, and edges between clusters then bundle through their islands' centres (hierarchical-edge-bundling lite, β 0.8). **Node size = PageRank** (√-scaled). **Seams**: each domain region, then each island, is rotated so terms shared with another domain face it. The whole map is turned so its long axis runs along the screen's. **By depth**: one lane per domain; depth rows are shared across lanes and wrap at ≈1.9·√(lane size); order within a row comes from 4 barycentric sweeps. **By time**: x = year (pre-1980 compressed 0.35×), one lane per domain, same-year terms stacked; undated terms hidden and counted. **3D**: `galaxyLayout` (pure, seeded, ≈150 ms) puts each domain's galaxy on a ring, with clusters as systems and shared terms pulled between galaxies. Height is a *soft*, jittered pull towards Depth, not a plane, so there's no flat floor. Positions are fixed (no live physics). One additive point cloud gives the glow (custom shader, fog-faded); the resting web is **one merged LineSegments**; 3d-force-graph draws only the focused links (arrows + 1 slow particle). Hubs get text sprites, and a camera swoop runs on open. **Multi-domain terms** get a split fill: vertical bands, one per enabled domain (2D hard-stop gradient; 3D a canvas texture shared per colour combination). This replaces the second-domain ring. **Motion**: dashes move only on hovered/selected/route edges, at 7 px/s, [3,9], 24 fps. No style transitions on the full map. A 45 ms hover intent, and `textureOnViewport` (pan/zoom move a snapshot, redrawn crisp on stop). All tunables live in `src/lib/explorer-config.ts`. **Measured** (headless Edge, RTX 3080, 1440×900; before → after): 2D pan 41 → 58 fps, zoom 40 → 60, hover 53 → 60 (worst frame 250 → 17 ms); 3D idle/orbit 4 → 60. With 2× CPU throttling (laptop proxy): pan 21 → 51, zoom 14 → 55, hover 47 → 60, 3D orbit 6 → 58. First-load main-thread block grew ≈460 → ≈950 ms (the full-graph layouts are computed up front). **Follow-up**: only the force layout is computed on load (depth/time on first switch, then cached; 3D on first open), and no-op route clearing is skipped: load block ≈915 → ≈520 ms. The legend starts closed, remembers the reader's choice (`atlas.explorer.legend`), is narrower (w-64) when open, and 2D fit (24 px padding) and the 3D camera (view offset + widened lens) frame the map in the part it leaves clear. **Owner feedback (second follow-up)**: *selection* dims everything not connected to the selected term — nodes, labels, edges, ribbons and names — and keeps the term, its neighbours and their edges fully lit, in 2D and 3D; hover previews the same, lighter. *Flow*: dots drift slowly along **every visible one-way edge, all the time** (`src/lib/explorer-flow.ts`: an overlay canvas, geometry re-sampled only on class/position changes, on-screen edges only, 30 fps; 3D: one point cloud, one particle per visible directed link); symmetric edges never move; nothing moves under reduced motion. *Spacing*: `separate()` pushes terms apart after layout until every pair is ≥ 2.2 × mean radius + a label's clearance (`EXPLORER.spacing`), per island in 2D and over all terms in 3D; islands grow to fit. *No seams*: shared terms sit in their own cluster like any other term (hubs such as secrets-management broke the facing rotation); 3D no longer pulls them between galaxies. *Backbone*: every `requires` and `strength: primary` edge (`GraphLink.primary`), plus each term's 2 strongest edges to terms sharing a domain in any cluster, plus the no-orphan fallback (954 of 1734 edges, was 572); resting backbone edges are straight, solid haystack lines (the cheapest Cytoscape draws), and those between clusters are quieter (α 0.12). *Look*: solid fills with a soft glow and a thin ring in the second domain's colour replace the split fill (2D, term page and legend); in 3D the shared term's glow takes the second colour. *Sidebar*: the legend moved into the left sidebar and every control group is a collapsible `<details>` (no new storage); the About "i" stays on the map and glides left of the docked term panel (above it, clear of its controls, when full width); "Open entry" left the sidebar (the panel's "Read more" does it), and "Tidy up" and "Fit to view" are gone (the map fits itself on open and on a layout switch). **Flow follow-up**: the 2D dots no longer rest during pan/zoom (they blinked out): they redraw from the live viewport in the next frame, so they stay locked to the map (2× throttle pan ≈50 fps, same as before). In 3D each visible one-way link carries a small comet (a head and a 3-point fading tail, family colour, 34 units/s, sized by distance and clamped to 4–18 px, dimmed with the selection; one `THREE.Points`, `EXPLORER.three.flow`); the old particle moved 0.05 units/s, i.e. not at all. The wheel and pinch zoom towards the cursor in 3D (`zoomToCursor`) and the canvas lab (pinch added); 2D already did. | The owner found both views laggy, the 3D cramped and garish, the line animation too fast, and the map a hairball with no overview. A stable, precomputed map means filters are cheap and nothing jumps. A backbone plus bundles reads as structure. Cytoscape's WebGL renderer was tried: no faster here, and nodes rendered nearly invisibly. Per-link tubes, cones and particles were what cost 3D its frames. |
| A87 | **Sign-in options: email hidden, providers by build variable, LinkedIn (OIDC) added.** `EMAIL_SIGNIN = false` in `src/lib/auth-config.ts` removes the email field everywhere (the code behind it stays; one line re-enables it). OAuth buttons come from `PUBLIC_AUTH_PROVIDERS` (repo variable in `deploy.yml`; comma-separated, allowlist `github`, `linkedin_oidc`, unknown names ignored, unset/empty/none recognised → `github`), shown in a fixed order. `signInWithGitHub` became `signInWith(provider)`: the same PKCE redirect to `/[lang]/account/`. Pure parser + `signInOptions` unit-tested. | The owner has no mail setup yet, and Supabase's built-in mailer reaches only project members, so the field could only fail. A provider button must not appear before the owner has configured it in Supabase and LinkedIn — a build-time list makes the site show exactly what is set up. `linkedin_oidc` because Supabase's plain "LinkedIn" provider is deprecated. |
| A88 | **Privacy page + a firm retention for the rate-limit hash.** `/[lang]/privacy/` (content in `src/lib/privacy.ts`, linked from the footer, the account page and the sign-in view): controller, every processing activity with what/why/where/Art. 6 basis/retention, sub-processors (GitHub Pages, Supabase EU, Cloudflare Workers AI — query text only, verified in `logic.ts` — GitHub/LinkedIn sign-in), every browser storage key (a test scans the source and fails on an unlisted key), rights, Datatilsynet. No consent banner: every key is strictly necessary or a remembered choice. Migration `20260926000000_search_rate_retention.sql`: `search_allow` now deletes expired counters on every call (was 2% of calls), plus a pg_cron job every 10 minutes, so a hashed-IP row is gone within ~12 minutes even without traffic. Account page gains "Download my progress" (JSON of `atlas:learner:v2`) for portability. | The hash of an IPv4 address can be brute-forced, so it is personal data and needs a real limit, not a traffic-dependent one. Portability needed an export of the one piece of personal data a learner creates. A test on the storage inventory keeps the page true as features add keys. |
| A89 | **Security hardening after a full review** (`design/SECURITY_REVIEW.md`, model in `docs/SECURITY.md`, reporting in `SECURITY.md`). (1) **Secret scanning in the gate**: `mise run audit` runs gitleaks (pinned in `mise.toml`, installed by the SHA-pinned mise action) over the whole git history with Supabase-specific rules (`sb_secret_`, `sbp_`, service_role JWTs); the only allowlist is the passage-hash lines of the two vector files, by exact path and line shape; `npm audit signatures` joins it. (2) **The build refuses to ship a secret or an uncovered page** (`app/integrations/dist-guard.mjs`, on `astro:build:done`, so gate and deploy): no secret key, access token, private key, non-`anon` JWT or value of a secret env var in `dist/`; every page has the CSP meta before any script, script-src without `unsafe-inline`/`unsafe-eval`, every inline script hashed, no inline handlers. (3) **CSP via Astro `security.csp`** (meta, since Pages has no headers): `script-src 'self'` + Astro's build-time hashes; `style-src 'self' 'unsafe-inline'` (Cytoscape/3d-force-graph inject `<style>` at run time, legends use `style=""`; with any hash present browsers would drop `unsafe-inline`); `connect-src 'self'` + the origins of `PUBLIC_SUPABASE_URL`/`PUBLIC_SEMANTIC_SEARCH_URL`; `default-src 'self'`, `img-src 'self' data:`, `object-src 'none'`, `base-uri`/`form-action 'self'`, `frame-src 'none'`. `frame-ancestors` is impossible in a meta policy, so Base.astro hides the page when framed. `referrer` = `strict-origin-when-cross-origin`. (4) **CI**: `backend` secrets step-scoped (none for checkout, tool installs, `npm ci`; seeding runs without the access token) and the job in a `backend` environment for the owner to restrict to `main`; `persist-credentials: false`; manual `deploy` on `main` only; `workflow_run` must come from this repository; Dependabot 5-day cooldown. (5) Source URLs `http(s)` only; root `.gitignore` covers `.env*`/`*.pem`/`*.key`; a migration revokes the trigger function's default EXECUTE and puts RLS on `private.search_rate`; the function answers `no-store` + `nosniff`. | The owner: "it'd be ridiculous to leak secrets". Nothing had leaked (history, dist and live RLS all checked); the real exposure was the Supabase account token visible to every third-party install script in `backend`. Hash-based script-src is stricter than the `'self'`-only policy first envisaged and costs nothing because Astro computes the hashes. The shared `cmaintz.github.io` origin (sessions in `localStorage` readable by the owner's other Pages sites) and repository settings (branch ruleset, environment secrets, private vulnerability reporting, SHA-pin enforcement, Dependabot security updates) need the owner — listed in `docs/SECURITY.md`. |
| A90 | **A hand-written question bank beside the generated quizzes.** New collection `questions`: `src/content/questions/<folder>/<cluster>.yaml`, a top-level `questions:` list (the glob loader wants an object per file). Each item: `id` (kebab, unique across the bank — it keys the learner record), `terms` (≥ 1, **namespaced `domain/id` only**, so no resolver or `audit` ambiguity), `kind` scenario / concept / compare / order / true-false, bilingual `stem`, 4 options (2 for true-false: True/Sandt, False/Falsk), `answer` index, bilingual `explanation` (why right and why the tempting options are wrong), `difficulty` 1–3, `sources`, `draft`. Schema `Question`/`QuestionFile` in `schema.ts`; pure rules in `lib/question-rules.ts`, run by `lint.ts`: Q1 schema, Q2 duplicate id, Q3 unknown/repeated term, Q4 answer index, Q5 blank text, Q6 options read the same, Q7 the stem names the correct option, Q8 explanation < 80 chars, Q9 true-false options; W9 file not a cluster, W10 no page can show it. Exempt from Closed Vocabulary. **Served** as `/questions-<lang>.json` (no sources; `answeredBy` = tested terms whose name/alias/slug *is* the correct option, derived at build); `Quiz.tsx` derives that URL from `graphUrl`, so the term page, the Explorer panel (TermPanel unchanged) and the study hub all get it; a missing bank falls back to generated questions. **Term pages / panel**: the term's hand-written questions first, never one in its `answeredBy` (the A79 rule), then generated ones to fill. **Sessions**: per term, a hand-written question that is new or due by its own record (never repeated in a session) else a generated one. **Learner**: optional `questions: Record<id, TermState>` beside `terms` — same Leitner step, parsed, merged per key by `mergeLearner` (absent until used, so older states and sync rows are unchanged; no migration). A hand-written answer updates every tested term plus the question's record. The explanation shows after answering with a "Read about X" link. **Content**: 180 draft questions over all 28 clusters, written by parallel research agents (security 62, cs 34, ai 64, platform 20); ~69 % scenarios in Danish workplaces. A length pass then made the correct option no longer the longest in all but the permutation-only `order` questions and one numeric one (it had been longest in 42 % of four-option questions). The session's web-search quota ran out mid-way, so agents fetched known primary URLs directly; where a primary source was unreachable (iso.org 403, EUR-Lex bot check) they cite the reachable text (Publications Office, gdpr-info.eu, artificialintelligenceact.eu) at a lower tier or left the fact out — e.g. no question relies on the 93-control / 4-theme Annex A count or on D-mærket's criteria count. | Owner: a hand-written bank of well-researched questions alongside the generated ones. Namespaced ids and a derived `answeredBy` make the A79 rule mechanical; a per-question record lets a well-known scenario wait while the term's other questions still come round. |
| A91 | **A hidden canvas Explorer lab** at `/<lang>/lab/5f69d757b126/` (EN + DA) so the owner can compare a hand-rolled renderer with the Cytoscape / 3d-force-graph Explorer. One `<canvas>`, a 2D context and "fake 3D" after the owner's Atlas.dc demo: every term has x/y/z, projected each frame with yaw/pitch + simple perspective, drawn back to front. **Layout** (pure, seeded, `lib/canvas-explorer.ts`, computed once): domains as regions on a grid sized by term count, a shared term in its primary (cluster's) domain, clusters seated across x and back-to-front in z, height a soft pull to Depth with a spread, a short relaxation, then a **minimum-spacing pass** so no two nodes overlap in the front view. **Modes**: Flat (front view, pan + zoom) and Depth (orbit, wheel zoom, optional slow auto-rotate, size/alpha/fog by z). **Look**: domain / cluster colours and vertical split bands with a thin outline for multi-domain terms (the owner wants the split tried here, not in real 3D), a soft glow sprite per colour, hub + hover labels placed by priority with collision rejection. **Edges**: backbone by default = the Explorer's `backbone()` ∪ every `requires` (graph.json folds strength into `weight`, so "primary" edges are the heavy ones it keeps), toggle to all; one-way edges carry slow pulses source → target, symmetric ones are static; none of it under reduced motion. Selection dims everything unconnected to 0.2 and opens the existing TermPanel (`?term=` kept). Nodes can be pulled and spring back (damped); positions never change. Domain / relationship toggles fade, never relayout; a search box focuses a term. **Controls** as a floating toolbar over a full-width map, a collapsible legend bottom-left, the About "i" top-right moving left of the open panel (owner's layout test). Page: `noindex` (so out of the sitemap), `tourAutoStart={false}`, linked from nowhere, no new storage keys. **Obscure, not secret** — the repository is public. Measured (headless Edge, 1440×900, rAF rate over 3 s): 60 fps in every lab state at 1× and 2× CPU throttle, as the Explorer; at 6× the lab holds 30–39 fps, the Explorer 3D 28–35, the Explorer 2D idles at 60 but its pan has 400 ms stalls. | Owner: compare a light, self-drawn map (the demo's feel: solid circles, glow, moving pulses, elastic nodes) with the library-based Explorer before deciding its future. A hidden page keeps the live Explorer untouched while another branch fixes it. |
| A92 | **Light theme, theme + language menus, footer at the bottom, sign-in-first account page.** (1) **Semantic tokens** in `global.css` (`--bg`, `--surface`, `--surface-2`, `--border`, `--border-strong`, `--border-hover`, `--fg`, `--fg-soft`, `--muted`, `--subtle`, `--accent`, `--focus`) with dark and light values, exposed through `@theme inline` as `bg-bg`, `text-fg`, `border-border`, `text-accent`…; every `neutral-*`/`white` class outside the map files was mapped mechanically (950→bg, 900→surface, 800/700→surface-2; text 50/100→fg, 200/300→fg-soft, 400→muted, 500/600→subtle), status colours got light values plus `dark:` variants. `dark:` is a custom variant keyed on `data-theme="dark"` (OS preference when there is no attribute, i.e. no JavaScript). Dark values keep the old look except `subtle` (#737373 → #909090) and the focus ring, which now meet AA. Every text token is ≥ 4.5:1 on bg/surface/surface-2 in both themes (worst 4.74). (2) **Choice**: header sun/moon menu, System / Light / Dark (`role="menuitemradio"`); System (the default) follows `prefers-color-scheme` live; Light/Dark stored as `atlas.theme` (listed on the privacy page, EN/DA). **No flash**: `public/theme-init.js`, a blocking `<script src>` as the first child of `<body>` — not in `<head>`, because Astro appends the CSP meta at the end of `<head>` and dist-guard (rightly) refuses any script before the policy; an external file needs no hash under `script-src 'self'`. Logic mirrored and unit-tested in `prefs.ts` (`parseTheme`, `resolveTheme`). (3) **Night map**: the Explorer, the canvas lab, the timeline chart and the term page's connection graph sit in a `data-theme="dark"` container, so they stay dark in the light theme (tokens hang off the attribute selector, so nesting re-scopes them); `Explorer.tsx`, `CanvasExplorer.tsx`, `Graph.tsx`, `GraphLegend.tsx`, `Timeline.tsx` and `explorer-*`/`graph-*` keep their literal dark classes — another agent owned them, and a light map canvas needs its own design pass (edge/label colours tuned for dark). **Superseded (owner request, PR feat/light-theme-complete): the night-map rule is gone.** The maps follow the page theme: in the light theme they sit on a warm cream ground (`--map-bg` #f6f1e7, `--map-glow` #faf6ee, a radial vignette via `.map-surface`) and every map element has a light-theme style. `graph-style.ts` gains `MapTheme` and `MAP_INK` (labels dark with a cream halo, ticks, selection, 3D background and fog, soft shadow instead of glow, flow-dot opacity) and a `theme` parameter (default dark, so server callers and tests are unchanged) on `domainColour`, `clusterColour`, `nodePaint`, `legendDomains`, `edgePaint`, `effectivePaint`, `domainBands`; cream shades are the same hues, deeper, darkened step by step until they reach 4.5:1 on cream (domains, also used as text) or 3:1 (clusters, WCAG 1.4.11); family colours have `FAMILY_COLOURS_LIGHT`. The theme is read from `data-theme` (`currentTheme`/`watchTheme` in `graph-cytoscape.ts`, the `useTheme` hook) and switches live, with no reload and no relayout: `retheme()` on the 2D and 3D maps swaps the stylesheet and element colour data (3D: background, fog, labels, and the additive glow, web and comets become multiplied tints, since light added to cream is invisible); the term graph and the canvas lab restyle in place. The timeline is server-rendered, so its domain colours are CSS custom properties (`.map-ink`: `--ink-d`/`--ink-l`) that follow the theme before hydration. Nav bar, control bar, legend, popovers, hover card and panels are on the A92 tokens. Domain-name watermarks on the 2D map stay decorative (0.45 opacity) and are not held to AA. **Scope narrowed (owner follow-up, PR fix/theme-scope-and-credits):** cream is only the Explorer's void (2D and 3D) and the canvas lab; the timeline chart and the term-page graph use the normal light surface (`--chart-bg` = `--surface` #f5f5f5, `.chart-surface`) with the same light palette, and stay the night map in the dark theme. The page and surface tokens are the A92 ones, unchanged. (4) **Language menu**: button + `role="menu"` of links (inline SVG flags — Windows renders flag emoji as letters — plus "English"/"Dansk"), same page via `swapLang`, arrows/Home/End/Esc/Tab, `#lang-switch` id kept for the tour. (5) **Footer**: body is a min-h-screen flex column, `main` flex-1 (wide pages unchanged). (6) **Account page signed out = the sign-in page**: heading "Sign in to Atlas", why, full-width provider buttons; the privacy link and "Download my progress" follow. OAuth already returned to the account page and the header "Sign in" already linked there. (7) Explorer sidebar: the selected-term card (cluster, name, summary) is gone — the term panel shows the same; its map actions (prerequisites, neighbourhood, expand, whole map) stay. | Owner requests. Tokens instead of a remapped neutral palette so the names say what a colour is for. |
| A93 | **Explorer: a floating control bar instead of the sidebar** (owner decision; amends A86's *Sidebar* follow-up). The map is full width. **Bar**: one rounded, centred row floating over the top of the map, styled after the canvas lab's toolbar, inset `left-14 right-14` so it never touches the edges or the About "i" (which keeps the top-right corner and still glides left of the docked term panel; the bar's right inset moves with it). It holds 2D/3D; Force/By depth/By time (2D) or **Auto-rotate** (3D, new: OrbitControls `autoRotate`, `EXPLORER.three.spinSpeed`, off by default and never under reduced motion); the domain toggles as pills; popovers for **Relationships** ("Show all relationships", the overview note and the family checkboxes), **Colour by** (Cluster / My knowledge) and **Route** (From/To, Find route, Clear, the route); and **Find a term** (`searchTerms` from `canvas-explorer.ts`; picking a term turns its domain on, leaves the time layout if it is undated, and selects it). One popover is open at a time; Esc closes it and returns focus to its button, as does a press outside the bar; the term panel's Esc handler stands down while a `[data-map-popover]` is open. The layout's note sits under the bar. Below 768 px the bar condenses to 2D/3D and a **Controls** sheet holding everything (rendered once, never duplicated). `role="group"`, `aria-pressed` toggles, `aria-expanded`/`aria-controls` on popover buttons. **Legend**: its own floating, collapsible box bottom-left (clear of the beta ribbon), still collapsed on a first visit and remembered in `atlas.explorer.legend` (no new storage keys). **Term panel actions**: the sidebar's selected-term card is gone; "Show prerequisites", "Neighbourhood", "Expand +1" and "Whole map" are a row of buttons under the panel's toolbar (`actions` prop; the lab's panel has none). **Hover card**: with a mouse (`(hover: hover) and (pointer: fine)`, so never on touch), resting on a term for 1.2 s (`EXPLORER.hoverCardMs`) shows a small card beside it — name, cluster chip, domain, and the summary already in graph.json (no fetch). Moving off, pressing, panning, zooming, orbiting or switching view hides it; 2D/3D report the term's screen position through a new `onPoint` callback; no card while auto-rotating (terms drift under the pointer). **Tour**: the three Explorer steps keep their `data-tour` anchors on the bar (layouts on 2D/3D, filters on the domain pills, route on the Route button) and fall back to `[data-explorer-bar]`. **Measured** (headless Edge, 1440×900, 2× CPU throttle): 2D pan ≈50 fps (47–50 over three runs), zoom 57, hover 59; 3D 60. | The owner likes the lab's floating toolbar: the map gets the whole width and the controls stay one slim row. Popovers keep parity with the sidebar without widening the bar. |
| A93a | **Explorer layout fix** (owner, amends A93): the legend moves to the map's top-left corner, level with the bar (collapsed to a pill, drops down to 60vh when open), and the bar is centred between equal `md:left-36 md:right-36` insets that clear it (sized for the Danish "Forklaring") and the About "i"; on phones the bar drops below the pill; the bar no longer moves when the term panel opens (only the "i" slides), the panel simply sits above it. **Colour by** is an inline Cluster | My knowledge toggle in the bar (no popover), so the bar never changes size. **Relationship types filter the overview only**: a selected term always shows all its relationships (2D, 3D and its neighbourhood); every type starts on except *used with* (amended by A95: contrasts and alternatives start off too); the backbone is recomputed over the types switched on (`backboneOf`), so an off type no longer takes a term's strongest-link slots; one line in the Relationships popover says so. The term panel keeps a single "Read more" (its toolbar). **One-row bar** (≈720 px at 1440): domain toggles are dot-only chips (name in tooltip and aria-label; labelled in the phone sheet), tighter segments, a short "Links" button, and "Find a term" is a search icon that opens the field (Esc or blur when empty closes it). With the panel docked the "i" also drops below the bar row, so it never covers the bar; while the legend is open the bar sits right of it (`md:left-[18rem]`), and on phones the legend sits below the bar. | The legend looked adrift mid-left; a bar that jumps when a term opens is disorienting. |
| A94 | **No em or en dashes anywhere a reader sees them** (owner rule). Every U+2014 em dash, U+2013 en dash and U+2015 horizontal bar in terms (all fields), articles, the question bank and the UI strings (`site.ts`, `privacy.ts`, `ui-extra.ts`, page titles " - Atlas", the empty-value placeholder, screen-reader announcements, inline page text) became hyphen-minus: " - " between clauses, "2018-2020" in ranges, "-" as a placeholder. The swap was mechanical (one character for one, verified by re-parsing every YAML file and frontmatter and checking no Markdown line turned into a list item or rule), not a per-sentence rewrite. **Guards**: lint **E12** fails on any of the three in a `src/content/**/*.{yaml,md}` file (raw text, so every field counts), and `ui-strings.test.ts` walks the exported values of `site`, `privacy`, `ui-extra`, `about` and `tour` (values, not source, so code comments may keep theirs). Components and pages are not unit-scanned; the Explorer files being reworked at the time (`Explorer.tsx`, `GraphLegend.tsx`, `AboutButton.tsx`, `explorer/index.astro`) are a follow-up. | The owner wants only normal dashes. One-for-one replacement keeps the diff reviewable and the text meaning intact; a lint rule and a string test stop them creeping back in from authoring tools that auto-convert "--". |
| A95 | **Explorer overview types, meaning search in "Find a term", drag feedback** (owner-approved types; amends A93a). **Overview**: with nothing selected the map draws only kind-of, part-of, implements, requires, mitigates, exploits, causes, mandates and supersedes (`OVERVIEW_FAMILIES` in graph-layout.ts = the structure, dependency, security, regulation and lineage families; `backboneOf`'s default). *Used with*, *contrasts with* and *alternative to* start unticked in the Relationships popover (its one line says which types the overview starts with, EN+DA); a selected term still shows all its relationships. **Find a term** behaves like the home search: instant name and alias matches (aliases now ride in `graph.json` when a term has any), and for a question or description the `semantic-search` function's near-best hits, fused by RRF and labelled "by meaning"; the debounced, abortable client is one hook (`use-semantic.ts`) and the fusion one pure function (`mergeHits`), both shared with the home search; without a backend, or on failure or timeout, names only. Picking a result selects the term and brings it into view (also when it is already selected; the 3D camera cuts instead of gliding under reduced motion). **Drag feedback** (`drag-feedback.ts`): an open hand at rest, a closed hand while panning (2D, 3D right or modified drag), a light rotate-arrow cursor while orbiting (3D), and a light ring with a crosshair and a dark outline following the mouse while dragging (not on touch; no fade under reduced motion); Cytoscape's dark press disc is switched off. Tunables in `EXPLORER.drag`. **About "i"**: hidden while the term panel is expanded, so it never covers the panel's own controls. | The overview read as a hairball of symmetric links; the owner picked the types that carry structure. A search box that only knew names disagreed with the home search. The old press marker was a dark disc on a dark map: dragging gave no visible feedback. |
| A96 | **A hidden visual lab for the Explorer** at `/<lang>/lab/5f69d757b126/2d/` and `/3d/` (EN + DA; noindex so out of the sitemap, linked from nowhere, no tour, like A91), so the owner can see and measure old visual effects live without deploying test branches. Each page renders the real `Explorer` (bar, legend, About "i", term panel unchanged) and a floating **Lab** panel whose toggles restyle in place, never relayout. **2D**: backbone curve (straight haystack, today / bezier / unbundled-bezier from the edge's own offset × a strength slider), source-to-target domain-colour gradients, one-way flow (overlay dots, today / the old Cytoscape `line-dash-offset` marching dashes on every visible one-way edge / none) with a speed slider, hover (today / the old `attachHover` that restyles every element, plus the old fade transitions), node glow, "show all", label density (none / hubs / culled, today / all) and the viewport snapshot. **3D**: merged lines (today) or a tube and arrow cone per link (old), link curvature (re-bends the merged web and the comets too), flow (comets, today / the old per-link particles / none) with speed, glow (point cloud, today / a sprite per term, old), bloom (`UnrealBloomPass` + `OutputPass` and an opaque scene background, else the transparent canvas turns grey), auto-rotate, fog, node spacing (the scene scales about its centre while terms and labels keep their size). **Meter**: rAF-based, current fps, 1% low (1000 / 99th-percentile frame time) and the longest frame over the last 5 s; **Benchmark** runs a scripted 5 s pan and zoom (2D, straight through `cy.viewport`, so every frame is a full redraw: the snapshot only applies to user gestures) or one orbit (3D) and reports the average fps and the active toggles. Toggles start from the address (`?curve=bezier&gradient=1&bench=1`), so a headless run is a list of URLs; nothing is stored (no new storage keys). **Production hook, optional and inert by default**: `Explorer` takes `lab?: { mode, showAll, onMaps }`; `createMap2D` returns `lab.dots` (a live copy of `EXPLORER.dots`) and `setDots`, `startDots` takes that copy; `createMap3D` returns `lab` (its three.js objects, the flow settings copy, the curve buffer and its link predicates). Old effects live in `ExplorerLab.tsx` / `explorer-lab.ts`, not behind production flags. **Measured** (headless Edge, 1440×900, 2× CPU throttle, RTX 3080; the PR has the table): 2D scripted pan and zoom (full redraws) ≈13 fps today, curves or gradients ≈11, marching dashes ≈9, all three ≈7, "show all" + dashes ≈5, no flow ≈16; 3D orbit 60 today, tubes ≈13, per-link particles ≈25, sprite glow ≈33, bloom 59 (1% low 30), tubes + particles + sprites ≈8. **Later additions (owner)**: the lab follows the page theme through the maps' own `retheme` (it re-reads the restyled base and lays its rules over it again; bloom is off on the cream map, where it only washes out); **emphasis by importance** (both views): importance = a per-type rank (requires, kind of, part of 1.0; mitigates, exploits, causes, mandates 0.85; implements, supersedes 0.7; contrasts with, alternative to 0.5; used with 0.35) × the square root of the link's own `weight` normalised to the heaviest, shown as opacity, colour (saturation and lightness towards the background), width (2D and 3D tubes) or all three, with a spread slider (importance ^ spread); **relayout** (lab only): a minimum-distance slider that re-runs the spacing pass (`createMap2D` keeps each island's fcose result and exposes `lab.relayout`; 3D re-spaces from the original layout) and sub-domain clusters (2D: islands 0.8 × and 70 px further apart; 3D: clusters 1.4 × out from their domain's centre and 0.7 × tighter, with optional faint cluster names); **cream-map contrast** sliders (term saturation / lightness, edge darkness / opacity, shadow strength, 2D label weight / halo) with a "Copy values" button. 3D exposes inert `webGain` / `webTint` per link, `linkLength`, `radius` and `labels` for these. | The owner wants to judge effects by eye and by frame rate before choosing; a lab that reuses the real component measures the real cost, and keeping the old code paths out of production keeps the Explorer diff to a few inert lines. |

Add rows as further solo decisions are made.

## Decisions first stated only in PR bodies (P1–P9)

Recorded here so they live in the repo (A70). Numbering follows `DECISIONS_REVIEW.md` §3.
Recording is not approval: the owner reviews them there.

| #   | Decision (PR)                                                                                                                                                                                          | Note                                                                                                                                  |
| --- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------- |
| P1  | **AI (28 terms) and platform (29 terms) domains added**, with cross-domain edges and `era` on 61 terms (PR #7).                                                                                        | A scope change against D3 / SPEC §11 ("still deferred"); on the owner's agenda (DECISIONS_REVIEW §7, item 1). SPEC §3/§11 now say so. |
| P2  | **Batch 5: 48 terms and three new clusters** (application-security, security-operations, web), edges on 21 existing terms (PR #11).                                                                    | Beyond the ordliste oracle, like A12/A30.                                                                                             |
| P3  | **Regulations' `era` = adoption year** (GDPR, NIS2, EU AI Act) (PR #11).                                                                                                                               | Conflicts with the Batch 4 rule "`era` = year a term entered common use"; one rule is on the owner's agenda (item 9).                 |
| P4  | **`nis2-loven` is its own term**, removed as an alias of `nis2` (PR #11).                                                                                                                              | The Danish implementing act is a different thing from the directive.                                                                  |
| P5  | **`cfcs` status `legacy`** — "tasks moved to SAMSIK in 2025" (PR #11).                                                                                                                                 | A factual claim for a human to verify.                                                                                                |
| P6  | **AI/platform articles follow a fixed structure** (history · how it works · a coordinator section with a worked scenario featuring a fictional Danish GRC student · common misunderstandings) (PR #9). | Tone choice.                                                                                                                          |
| P7  | **Browser checks were headless-Edge smoke tests run by hand** (PRs #3, #5, #6, #12).                                                                                                                   | Not in the gate; see DECISIONS_REVIEW F6.                                                                                             |
| P8  | **A fresh-context AI review is the only code review before merge**; PRs were merged under the owner's account (PR #1 onward).                                                                          | Who approves merges is on the owner's agenda (item 11).                                                                               |
| P9  | **PR #12's body is stale**: its A44–A48 numbering and "runtime from jsDelivr" were superseded by A51–A55 as merged.                                                                                    | History only; A51–A55 are what shipped.                                                                                               |

## Batch 4 (AI + platform) decisions

- **`era` = the year a term entered common use** (mainstream adoption), not the year of
  first invention or publication — e.g. MFA is dated 2011; the GDPR controller/processor
  roles are dated to Directive 95/46/EC, where they originate.
- **No `security` domain on ML/LLM terms.** Their security relevance is carried by edges
  (prompt injection, data poisoning, deepfakes, AI risk), so the domain tag stays honest.
- **`eu-ai-act` and `ai-governance` have no `layer`** — they are regulation and management
  practice, not a layer of an AI system.
- **Proper nouns dropped from English prose to satisfy Closed Vocabulary (E1)** — e.g. the
  EU Cyber Resilience Act, NTIA, and "Kubernetes" in the GitOps prose; they stay in
  `sources`. English has no compound splitting, so such names cannot pass E1 otherwise.
- **Auto-linking: a stop-list for everyday words plus domain-aware collision resolution**
  (PR #6) — a name shared by two terms links to the one in the page's own domain.
- **Danish "mod" is a compare separator only after "sammenlign"** (PR #6) — bare "mod"
  is an everyday word and otherwise produced false compare intents.
- **EU AI Act dates follow the Digital Omnibus on AI (Regulation (EU) 2026/1744)**: Annex
  III high-risk duties apply from 2 December 2027, Annex I (AI built into products) from
  2 August 2028; Article 50 openness duties still from 2 August 2026.
